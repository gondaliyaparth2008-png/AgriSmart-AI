# ============================================================
#  AgriSmart AI — Backend Dependencies
# ============================================================
#
#  Install:   pip install -r requirements.txt
#
# ============================================================

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.115.0

# ── ASGI Server ───────────────────────────────────────────────
uvicorn[standard]==0.30.6

# ── Multipart / file upload ───────────────────────────────────
python-multipart==0.0.9

# ── Data validation & settings ────────────────────────────────
pydantic==2.8.2
pydantic-settings==2.4.0

# ── Image processing ──────────────────────────────────────────
Pillow==10.4.0

# ── Async HTTP client (live weather API + OWM) ────────────────
httpx==0.27.2
requests==2.32.3              # sync fallback / utility use

# ── Database (Async SQLAlchemy) ───────────────────────────────
sqlalchemy==2.0.35            # SQLAlchemy 2.x with async support
aiosqlite==0.20.0             # SQLite async driver (local development)
asyncpg==0.31.0               # PostgreSQL async driver (required for Supabase / production)

# ── Supabase client SDK ───────────────────────────────────────────────────────
supabase==2.9.1               # Python Supabase client (Auth, Storage, Realtime)

# ── Google Gemini AI (farmer assistant) ───────────────────────
google-genai>=1.0.0           # Official Google GenAI SDK (HTTP-based)

# ── Testing ───────────────────────────────────────────────────
pytest==8.3.2
pytest-asyncio==0.23.8

# ── Optional — Real model backends (uncomment to use) ─────────
# onnxruntime==1.19.0          # ONNX inference (recommended, cross-framework)
# numpy==1.26.4                # Required by ONNX / PyTorch / Keras backends
# torch==2.4.0                 # PyTorch model loading
# torchvision==0.19.0
# tensorflow==2.17.0           # Keras / TF model loading
